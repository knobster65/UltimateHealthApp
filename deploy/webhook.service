[Unit]
Description=GitHub Webhook Deploy Receiver
After=network.target

[Service]
User=root
ExecStart=/usr/bin/python3 /usr/local/bin/webhook_receiver
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
